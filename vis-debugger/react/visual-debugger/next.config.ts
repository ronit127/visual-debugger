import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  turbopack: {
    root: "/Users/Felix/Documents/GitHub/visual-debugger/vis-debugger/react/visual-debugger",
  },
};

export default nextConfig;
