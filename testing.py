 # with contextlib.redirect_stdout(output):(maybe)
    # try:
    #     pass
    # except Exception as e:
    #     return jsonify({
    #         'status': 'error',
    #         'error': str(e)
    #     })